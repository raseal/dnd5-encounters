doctrine_migrations:
    migrations_paths:
        # namespace is arbitrary but should be different from App\Migrations
        # as migrations classes should NOT be autoloaded
        'Shared\Infrastructure\Persistence\migrations': '%kernel.project_dir%/../../src/Shared/Infrastructure/Persistence/migrations'
    enable_profiler: '%kernel.debug%'
